/**
 * Phase 0 placeholder. No application pages/features are implemented
 * yet — this exists only to confirm the Vite + React + TypeScript
 * foundation renders. Replaced starting with the Authentication
 * module's frontend work.
 */
export default function App() {
  return (
    <main>
      <h1>HELIOS</h1>
      <p>Health Event &amp; Longitudinal Intelligence Operating System</p>
      <p>Foundation scaffold — no features implemented yet.</p>
    </main>
  );
}
